'use strict';

describe('linqinfer.samples module', function () {

    beforeEach(module('linqinfer.samples'));

    describe('samples controller', function () {

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('SamplesCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});